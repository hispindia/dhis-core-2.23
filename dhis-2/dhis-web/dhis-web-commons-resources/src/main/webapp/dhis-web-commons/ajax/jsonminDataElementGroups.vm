#set( $size = $dataElementGroups.size() )
{"dataElementGroups":[#foreach( $group in $dataElementGroups ){"id":$!{group.id},"name":"$!encoder.jsonEncode( ${group.name} )"}#if( $velocityCount < $size ),#end#end]}