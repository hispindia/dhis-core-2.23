#set( $size = $object.size() )
{"dataElementGroups":[#foreach( $dataElementGroup in $object ) {"id":"$!{dataElementGroup.id}","name":"$!encoder.jsEncode( ${dataElementGroup.name} )" }#if( $velocityCount < $size ),#end#end]}