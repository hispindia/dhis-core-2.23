#set( $size = $object.size() )
{"dataElements":[#foreach( $dataElement in $object ){"id":"$!{dataElement.id}","name":"$!encoder.jsEncode( ${dataElement.name} )","shortName":"$!encoder.jsEncode( ${dataElement.shortName} )"}#if( $velocityCount < $size ),#end#end]}